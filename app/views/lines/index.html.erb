<div id="line-list-container">
  <% @lines.each do |line| %>
  	<div class="line-parent" id="<%= line.name.downcase %>">
	  	<%= link_to line_path(line) do %>
		    <div class="line-child">
		    	<%= line.name %>
		    </div>
	    <% end %>
	  </div>
  <% end %>
</div>